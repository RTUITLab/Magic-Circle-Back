// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/0B1t322/Magic-Circle/ent/superadmin"
)

// SuperAdminCreate is the builder for creating a SuperAdmin entity.
type SuperAdminCreate struct {
	config
	mutation *SuperAdminMutation
	hooks    []Hook
}

// SetLogin sets the "login" field.
func (sac *SuperAdminCreate) SetLogin(s string) *SuperAdminCreate {
	sac.mutation.SetLogin(s)
	return sac
}

// SetPassword sets the "password" field.
func (sac *SuperAdminCreate) SetPassword(s string) *SuperAdminCreate {
	sac.mutation.SetPassword(s)
	return sac
}

// SetEmail sets the "email" field.
func (sac *SuperAdminCreate) SetEmail(s string) *SuperAdminCreate {
	sac.mutation.SetEmail(s)
	return sac
}

// SetNillableEmail sets the "email" field if the given value is not nil.
func (sac *SuperAdminCreate) SetNillableEmail(s *string) *SuperAdminCreate {
	if s != nil {
		sac.SetEmail(*s)
	}
	return sac
}

// Mutation returns the SuperAdminMutation object of the builder.
func (sac *SuperAdminCreate) Mutation() *SuperAdminMutation {
	return sac.mutation
}

// Save creates the SuperAdmin in the database.
func (sac *SuperAdminCreate) Save(ctx context.Context) (*SuperAdmin, error) {
	var (
		err  error
		node *SuperAdmin
	)
	if len(sac.hooks) == 0 {
		if err = sac.check(); err != nil {
			return nil, err
		}
		node, err = sac.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SuperAdminMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = sac.check(); err != nil {
				return nil, err
			}
			sac.mutation = mutation
			if node, err = sac.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(sac.hooks) - 1; i >= 0; i-- {
			if sac.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = sac.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, sac.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (sac *SuperAdminCreate) SaveX(ctx context.Context) *SuperAdmin {
	v, err := sac.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sac *SuperAdminCreate) Exec(ctx context.Context) error {
	_, err := sac.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sac *SuperAdminCreate) ExecX(ctx context.Context) {
	if err := sac.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (sac *SuperAdminCreate) check() error {
	if _, ok := sac.mutation.Login(); !ok {
		return &ValidationError{Name: "login", err: errors.New(`ent: missing required field "login"`)}
	}
	if _, ok := sac.mutation.Password(); !ok {
		return &ValidationError{Name: "password", err: errors.New(`ent: missing required field "password"`)}
	}
	return nil
}

func (sac *SuperAdminCreate) sqlSave(ctx context.Context) (*SuperAdmin, error) {
	_node, _spec := sac.createSpec()
	if err := sqlgraph.CreateNode(ctx, sac.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (sac *SuperAdminCreate) createSpec() (*SuperAdmin, *sqlgraph.CreateSpec) {
	var (
		_node = &SuperAdmin{config: sac.config}
		_spec = &sqlgraph.CreateSpec{
			Table: superadmin.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: superadmin.FieldID,
			},
		}
	)
	if value, ok := sac.mutation.Login(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: superadmin.FieldLogin,
		})
		_node.Login = value
	}
	if value, ok := sac.mutation.Password(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: superadmin.FieldPassword,
		})
		_node.Password = value
	}
	if value, ok := sac.mutation.Email(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: superadmin.FieldEmail,
		})
		_node.Email = value
	}
	return _node, _spec
}

// SuperAdminCreateBulk is the builder for creating many SuperAdmin entities in bulk.
type SuperAdminCreateBulk struct {
	config
	builders []*SuperAdminCreate
}

// Save creates the SuperAdmin entities in the database.
func (sacb *SuperAdminCreateBulk) Save(ctx context.Context) ([]*SuperAdmin, error) {
	specs := make([]*sqlgraph.CreateSpec, len(sacb.builders))
	nodes := make([]*SuperAdmin, len(sacb.builders))
	mutators := make([]Mutator, len(sacb.builders))
	for i := range sacb.builders {
		func(i int, root context.Context) {
			builder := sacb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SuperAdminMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, sacb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, sacb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, sacb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (sacb *SuperAdminCreateBulk) SaveX(ctx context.Context) []*SuperAdmin {
	v, err := sacb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sacb *SuperAdminCreateBulk) Exec(ctx context.Context) error {
	_, err := sacb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sacb *SuperAdminCreateBulk) ExecX(ctx context.Context) {
	if err := sacb.Exec(ctx); err != nil {
		panic(err)
	}
}
