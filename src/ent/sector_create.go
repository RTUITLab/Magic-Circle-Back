// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/0B1t322/Magic-Circle/ent/adjacenttable"
	"github.com/0B1t322/Magic-Circle/ent/sector"
)

// SectorCreate is the builder for creating a Sector entity.
type SectorCreate struct {
	config
	mutation *SectorMutation
	hooks    []Hook
}

// SetCoords sets the "coords" field.
func (sc *SectorCreate) SetCoords(s string) *SectorCreate {
	sc.mutation.SetCoords(s)
	return sc
}

// SetDescription sets the "description" field.
func (sc *SectorCreate) SetDescription(s string) *SectorCreate {
	sc.mutation.SetDescription(s)
	return sc
}

// SetNillableDescription sets the "description" field if the given value is not nil.
func (sc *SectorCreate) SetNillableDescription(s *string) *SectorCreate {
	if s != nil {
		sc.SetDescription(*s)
	}
	return sc
}

// AddAdjacentTableIDs adds the "AdjacentTables" edge to the AdjacentTable entity by IDs.
func (sc *SectorCreate) AddAdjacentTableIDs(ids ...int) *SectorCreate {
	sc.mutation.AddAdjacentTableIDs(ids...)
	return sc
}

// AddAdjacentTables adds the "AdjacentTables" edges to the AdjacentTable entity.
func (sc *SectorCreate) AddAdjacentTables(a ...*AdjacentTable) *SectorCreate {
	ids := make([]int, len(a))
	for i := range a {
		ids[i] = a[i].ID
	}
	return sc.AddAdjacentTableIDs(ids...)
}

// Mutation returns the SectorMutation object of the builder.
func (sc *SectorCreate) Mutation() *SectorMutation {
	return sc.mutation
}

// Save creates the Sector in the database.
func (sc *SectorCreate) Save(ctx context.Context) (*Sector, error) {
	var (
		err  error
		node *Sector
	)
	if len(sc.hooks) == 0 {
		if err = sc.check(); err != nil {
			return nil, err
		}
		node, err = sc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SectorMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = sc.check(); err != nil {
				return nil, err
			}
			sc.mutation = mutation
			if node, err = sc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(sc.hooks) - 1; i >= 0; i-- {
			if sc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = sc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, sc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (sc *SectorCreate) SaveX(ctx context.Context) *Sector {
	v, err := sc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sc *SectorCreate) Exec(ctx context.Context) error {
	_, err := sc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sc *SectorCreate) ExecX(ctx context.Context) {
	if err := sc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (sc *SectorCreate) check() error {
	if _, ok := sc.mutation.Coords(); !ok {
		return &ValidationError{Name: "coords", err: errors.New(`ent: missing required field "coords"`)}
	}
	return nil
}

func (sc *SectorCreate) sqlSave(ctx context.Context) (*Sector, error) {
	_node, _spec := sc.createSpec()
	if err := sqlgraph.CreateNode(ctx, sc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (sc *SectorCreate) createSpec() (*Sector, *sqlgraph.CreateSpec) {
	var (
		_node = &Sector{config: sc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: sector.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: sector.FieldID,
			},
		}
	)
	if value, ok := sc.mutation.Coords(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: sector.FieldCoords,
		})
		_node.Coords = value
	}
	if value, ok := sc.mutation.Description(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: sector.FieldDescription,
		})
		_node.Description = value
	}
	if nodes := sc.mutation.AdjacentTablesIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   sector.AdjacentTablesTable,
			Columns: []string{sector.AdjacentTablesColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: adjacenttable.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// SectorCreateBulk is the builder for creating many Sector entities in bulk.
type SectorCreateBulk struct {
	config
	builders []*SectorCreate
}

// Save creates the Sector entities in the database.
func (scb *SectorCreateBulk) Save(ctx context.Context) ([]*Sector, error) {
	specs := make([]*sqlgraph.CreateSpec, len(scb.builders))
	nodes := make([]*Sector, len(scb.builders))
	mutators := make([]Mutator, len(scb.builders))
	for i := range scb.builders {
		func(i int, root context.Context) {
			builder := scb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SectorMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, scb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, scb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, scb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (scb *SectorCreateBulk) SaveX(ctx context.Context) []*Sector {
	v, err := scb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (scb *SectorCreateBulk) Exec(ctx context.Context) error {
	_, err := scb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (scb *SectorCreateBulk) ExecX(ctx context.Context) {
	if err := scb.Exec(ctx); err != nil {
		panic(err)
	}
}
