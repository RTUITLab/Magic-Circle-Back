// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/0B1t322/Magic-Circle/ent/institute"
)

// Institute is the model entity for the Institute schema.
type Institute struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the InstituteQuery when eager-loading is set.
	Edges InstituteEdges `json:"edges"`
}

// InstituteEdges holds the relations/edges for other nodes in the graph.
type InstituteEdges struct {
	// Directions holds the value of the Directions edge.
	Directions []*Direction `json:"Directions,omitempty"`
	// Admins holds the value of the Admins edge.
	Admins []*Admin `json:"Admins,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// DirectionsOrErr returns the Directions value or an error if the edge
// was not loaded in eager-loading.
func (e InstituteEdges) DirectionsOrErr() ([]*Direction, error) {
	if e.loadedTypes[0] {
		return e.Directions, nil
	}
	return nil, &NotLoadedError{edge: "Directions"}
}

// AdminsOrErr returns the Admins value or an error if the edge
// was not loaded in eager-loading.
func (e InstituteEdges) AdminsOrErr() ([]*Admin, error) {
	if e.loadedTypes[1] {
		return e.Admins, nil
	}
	return nil, &NotLoadedError{edge: "Admins"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Institute) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case institute.FieldID:
			values[i] = new(sql.NullInt64)
		case institute.FieldName:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Institute", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Institute fields.
func (i *Institute) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for j := range columns {
		switch columns[j] {
		case institute.FieldID:
			value, ok := values[j].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			i.ID = int(value.Int64)
		case institute.FieldName:
			if value, ok := values[j].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[j])
			} else if value.Valid {
				i.Name = value.String
			}
		}
	}
	return nil
}

// QueryDirections queries the "Directions" edge of the Institute entity.
func (i *Institute) QueryDirections() *DirectionQuery {
	return (&InstituteClient{config: i.config}).QueryDirections(i)
}

// QueryAdmins queries the "Admins" edge of the Institute entity.
func (i *Institute) QueryAdmins() *AdminQuery {
	return (&InstituteClient{config: i.config}).QueryAdmins(i)
}

// Update returns a builder for updating this Institute.
// Note that you need to call Institute.Unwrap() before calling this method if this Institute
// was returned from a transaction, and the transaction was committed or rolled back.
func (i *Institute) Update() *InstituteUpdateOne {
	return (&InstituteClient{config: i.config}).UpdateOne(i)
}

// Unwrap unwraps the Institute entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (i *Institute) Unwrap() *Institute {
	tx, ok := i.config.driver.(*txDriver)
	if !ok {
		panic("ent: Institute is not a transactional entity")
	}
	i.config.driver = tx.drv
	return i
}

// String implements the fmt.Stringer.
func (i *Institute) String() string {
	var builder strings.Builder
	builder.WriteString("Institute(")
	builder.WriteString(fmt.Sprintf("id=%v", i.ID))
	builder.WriteString(", name=")
	builder.WriteString(i.Name)
	builder.WriteByte(')')
	return builder.String()
}

// Institutes is a parsable slice of Institute.
type Institutes []*Institute

func (i Institutes) config(cfg config) {
	for _i := range i {
		i[_i].config = cfg
	}
}
